import styled from "@emotion/styled";
import buttonFactory from "../buttonFactory";

const NegativeButton = styled(buttonFactory)({
  backgroundColor: "grey",
  borderColor: "grey"
});

export default NegativeButton;
