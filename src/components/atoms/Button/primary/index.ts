import styled from "@emotion/styled";
import buttonFactory from "../buttonFactory";

const PrimaryButton = styled(buttonFactory)({
  backgroundColor: "#007bff",
  borderColor: "#007bff"
});

export default PrimaryButton;
